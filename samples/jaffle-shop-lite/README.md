# Jaffle Shop Lite

This is a bundled sample project for trying Lineage MCP safely.

Suggested checks:

- `list_tables`
- `list_lineage` for `stg_orders`
- `check_impact` for renaming `stg_orders`

This sample includes:

- `models/staging/stg_orders.sql`
- `models/marts/orders.sql`
- `target/manifest.json`
